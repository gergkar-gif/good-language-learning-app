{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "units.schema.json",
  "title": "Level unit table",
  "description": "How a level's units are named, ordered, and assembled from lesson stems -- read by build-manifest.py's build_curriculum() to generate curriculum.json. One file per level (content/<lang>/curriculum/units/<level>.json), an ordered array of units. Position within a track is this array's own order, not a stored number: two units, or two entries sharing the same `track`, are told apart purely by where they sit in the list. A level with no file here falls back to build-manifest.py's auto_group_units() instead (plain-numbered lesson files with no explicit table, e.g. Hungarian A1/A2).",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": ["title", "stems"],
    "additionalProperties": false,
    "properties": {
      "title": {
        "type": "string",
        "minLength": 1,
        "description": "Human-authored unit name shown in the Learn tab -- not derivable from the stems, so this is the one thing this file exists to carry.",
        "examples": ["Talking About Your Trip", "The Imperfect Tense"]
      },
      "stems": {
        "type": "array",
        "minItems": 1,
        "items": { "type": "string", "minLength": 1 },
        "description": "Lesson file stems, in teaching order, exactly as they appear on disk under lessons/<level>/ (without the .json extension) -- e.g. \"a2-imperfectobasico-01\" for lessons/a2/a2-imperfectobasico-01.json. A stem naming a file that doesn't exist yet is fine (build-manifest.py's load_lesson_entry() just skips it), so a unit can be declared before every lesson in it is written.",
        "examples": [["a2-01-01", "a2-01-02", "a2-01-03", "a2-01-04", "a2-01-05", "a2-01-consolidation"]]
      },
      "track": {
        "type": "string",
        "minLength": 1,
        "description": "Only for a level that runs more than one parallel track (see LEVEL_TRACKS in build-manifest.py, e.g. Spanish B1's Core/Latin America) -- which track this unit belongs to. Omit entirely for a single-track level.",
        "examples": ["core", "latam", "citizenship"]
      }
    }
  }
}
